[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "webtools"
version = "0.1.0"
description = "Personal web utilities for fetching and extracting content"
requires-python = ">=3.9"
dependencies = [
    "trafilatura"
]
